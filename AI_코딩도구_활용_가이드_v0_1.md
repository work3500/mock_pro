# AI 코딩도구 활용 가이드

| 항목 | 내용 |
|---|---|
| 문서 번호 | AI-GUIDE-001 |
| 버전 | v0.1 (초안) |
| 작성 | AX추진실 |
| 작성일 | 2026.06 |
| 적용 대상 | 기술 인력 전원 |
| 상위 규범 | 「인공지능 활용 및 관리 요령」 v1.0-r6 / 「AI 도구 활용 정책」 v2.3 (AI-GOV-003-001) |

---

## 목차

- [제1장 개요](#제1장-개요)
  - [1.1 목적](#11-목적)
  - [1.2 적용 범위](#12-적용-범위)
  - [1.3 상위 규범과의 관계](#13-상위-규범과의-관계)
  - [1.4 용어 정의](#14-용어-정의)
- [제2장 승인 도구 현황 및 활용 방식](#제2장-승인-도구-현황-및-활용-방식)
  - [2.1 승인 도구 목록 및 Tier](#21-승인-도구-목록-및-tier)
  - [2.2 활용 방식별 분류 및 특성](#22-활용-방식별-분류-및-특성)
  - [2.3 신규 도구 도입 및 변경 절차](#23-신규-도구-도입-및-변경-절차)
  - [2.4 미승인 도구 사용 금지](#24-미승인-도구-사용-금지)

---

## 제1장 개요

### 1.1 목적

이 가이드는 한국무역정보통신(이하 "회사") 기술 인력이 업무에 활용하는 AI 코딩도구를 안전하고 효과적으로 사용할 수 있도록 실무 기준을 제공하는 것을 목적으로 한다.

AI 코딩도구는 코드 생성·완성·리뷰·디버깅·문서화 등 개발 전 주기에 걸쳐 생산성을 향상시키는 반면, 다음과 같은 고유한 위험을 함께 수반한다.

- **소스코드·크리덴셜 유출**: 코드를 입력하는 과정에서 인증 정보, 핵심 비즈니스 로직이 외부 AI 서비스로 전송될 수 있다.
- **코딩 에이전트의 무단 시스템 조작**: 자율 실행 모드에서 AI 에이전트가 파일 삭제, 외부 데이터 전송 등을 수행할 수 있다.
- **플러그인·MCP 서버를 통한 공급망 위협**: 검증되지 않은 IDE 플러그인이나 MCP 서버가 악의적 코드를 실행하거나 데이터를 탈취할 수 있다.

이 가이드는 위 세 가지 위험 영역을 중심으로 도구 활용 기준을 제시하며, 「인공지능 활용 및 관리 요령」(이하 "요령") 제16조(입력 금지 정보), 제42조(AI 에이전트 권한 통제), 제43조(공급망 보안)의 실무 적용 기준서로 기능한다.

---

### 1.2 적용 범위

#### 적용 대상 인원

이 가이드는 다음 각 호에 해당하는 자에게 적용한다. (요령 제3조 준용)

1. 회사 정규직·계약직·파견직 임직원 전원
2. 상주 협력업체 소속 인력 중 회사 시스템에 접근하는 자
3. 회사가 발주한 용역·컨설팅 사업에 참여하는 인력으로서 회사 정보를 취급하는 자

협력업체 소속 인력은 회사 업무 수행 전 이 가이드에 관한 교육을 이수하고 준수 서약서를 제출하여야 한다. (요령 제47조 ②)

#### 적용 대상 도구

이 가이드는 회사가 승인한 AI 코딩도구 전체에 적용하며, 현재 승인 도구 목록은 「AI 도구 승인 목록」(AI-LIST-001)에서 확인한다.

#### 적용 행위 범위

이 가이드는 다음 각 호의 AI 코딩도구 이용 행위에 적용한다. (요령 제3조 ② 준용)

1. 회사 소유 단말, 네트워크, 계정을 통한 AI 코딩도구 이용
2. 개인 단말을 이용하더라도 회사 업무 수행 목적의 AI 코딩도구 이용
3. 회사가 직접 개발·운영하는 AI 기반 코딩 지원 시스템

> **주의**: 개인 단말에서의 업무 목적 이용도 이 가이드의 적용을 받는다. 개인 단말로 업무 관련 코드를 AI 도구에 입력하는 행위는 회사 단말 이용과 동일한 기준이 적용된다.

---

### 1.3 상위 규범과의 관계

이 가이드는 아래 규범 체계 하에 위치하며, 상위 규범과 충돌하는 경우 상위 규범을 우선 적용한다.

```
┌─────────────────────────────────────────┐
│  KTNET 윤리강령 (최상위)                │
│  ← 모든 사규에 우선 (부칙 7항)         │
└───────────────────┬─────────────────────┘
                    │
┌───────────────────▼─────────────────────┐
│  인공지능 활용 및 관리 요령  v1.0-r6    │
│  ← AI 활용·관리 최상위 요령             │
└───────┬───────────────────┬─────────────┘
        │                   │
┌───────▼───────┐   ┌───────▼──────────────┐
│ 정보보호관리  │   │ 개인정보보호세칙      │
│ 요령 / 세칙   │   │ 서비스보안성 심의 절차│
└───────────────┘   └──────────────────────┘
                    │
┌───────────────────▼─────────────────────┐
│  AI 도구 활용 정책  v2.3                │
│  (AI-GOV-003-001)                       │
│  ← 코딩도구 포함 AI 도구 전체 시행세칙 │
└───────────────────┬─────────────────────┘
                    │ 위임
┌───────────────────▼─────────────────────┐
│  AI 코딩도구 활용 가이드  ← 본 문서    │
│  (AI-GUIDE-001)                         │
└─────────────────────────────────────────┘
```

이 가이드에서 정하지 아니한 사항은 「AI 도구 활용 정책」(AI-GOV-003-001) 및 「인공지능 활용 및 관리 요령」을 따른다.

---

### 1.4 용어 정의

| 용어 | 정의 |
|---|---|
| **AI 코딩도구** | 소프트웨어 코드의 생성·완성·리뷰·설명·디버깅을 지원하는 AI 도구의 총칭. 웹 채팅 방식, IDE 플러그인, 코딩 에이전트를 모두 포함한다. |
| **코딩 에이전트** | 사용자의 지시를 받아 파일 시스템·터미널·외부 API 등을 자율적으로 호출하여 일련의 개발 작업을 수행하는 AI. (요령 제2조 5호의 "AI 에이전트"에 해당) |
| **IDE 플러그인** | JetBrains AI Pro, Cursor 등 통합개발환경(IDE)에 통합된 AI 보조 기능. 인라인 자동완성, 코드 제안, Agent 모드를 제공한다. |
| **웹 채팅 방식** | Claude.ai, ChatGPT 등 브라우저 기반 대화형 인터페이스를 통해 코드 생성·분석·문서화 등 개발 업무를 지원하는 방식. |
| **Tier** | 「AI 도구 활용 정책」의 도구 등급. Tier A(엔터프라이즈 계약), Tier B(개인 계정), Tier S(사내 전용)로 구분하며, 요령의 등급 2, 등급 1, 등급 3에 각각 대응한다. |
| **Level** | AI에 입력하는 데이터의 민감도 등급. Level 1(일반정보), Level 2(대외비), Level 3(비밀)로 구분한다. (요령 제12조) |
| **ZDR** | Zero Data Retention. AI 서비스 제공사가 사용자의 입력 데이터를 모델 학습 및 서비스 개선에 이용하지 않음을 계약상 보장하는 조건. Tier A 도구의 필수 요건이다. |
| **통제 워크스페이스** | AX추진실이 관리자(Admin) 권한을 보유한 Team/Enterprise 플랜 계정. 학습 OFF(ZDR) 설정, 사용자 추가·삭제, 사용 로그 조회가 가능하다. |
| **MCP 서버** | Model Context Protocol 기반으로 AI 에이전트에 외부 도구·데이터를 연결하는 서버. 코딩 에이전트(Claude Code 등)의 기능을 확장하는 데 사용된다. (요령 제43조 명시 대상) |
| **AI-BOM** | AI 시스템 구성 요소(모델·라이브러리·외부 API·MCP 서버 등)의 목록과 출처·버전·라이선스·보안 상태를 기록한 문서. (요령 제2조 13호) |
| **크리덴셜** | API 키, 비밀번호, 인증서, 접속 토큰, 환경변수 파일(.env) 등 시스템 접근에 사용되는 인증 정보의 총칭. |
| **CLAUDE.md** | Claude Code가 프로젝트 시작 시 자동으로 로드하는 프로젝트 지시 파일. 작업 범위·금지 사항·코딩 규칙 등을 명시하여 에이전트 행동을 사전 제약한다. |

---

## 제2장 승인 도구 현황 및 활용 방식

### 2.1 승인 도구 목록 및 Tier

회사가 현재 승인한 AI 코딩도구는 아래와 같다. 최신 승인 상태 및 허용 조건의 변경은 「AI 도구 승인 목록」(AI-LIST-001)이 기준이며, 해당 목록이 아래 표에 우선한다.

| 도구 | 활용 방식 | Tier | 요령 등급 | 학습 이용 | 최대 허용 Level |
|---|---|---|---|---|---|
| Claude.ai (Team / Premium) | 웹 채팅 · 코딩 에이전트(Claude Code) | A | 등급 2 | OFF (ZDR) | Level 2 (조건부) |
| JetBrains AI Pro | IDE 플러그인 | A | 등급 2 | OFF | Level 2 (조건부) |
| Cursor (Team) | IDE 플러그인 | A | 등급 2 | OFF | Level 2 (조건부) |
| GitHub Copilot (기업 계약) | IDE 플러그인 | A | 등급 2 | OFF | Level 2 (조건부) |
| ChatGPT Business | 웹 채팅 | A | 등급 2 | OFF | Level 2 (조건부) |

**Level 허용 기준 요약**

| Level | 데이터 유형 | Tier A 도구 허용 여부 |
|---|---|---|
| Level 1 (일반정보) | 공개 가능 정보, 공개 라이브러리 활용 코드 | ✅ 허용 |
| Level 2 (대외비) | 업무 로직, 내부 API, 사업 계획 관련 코드 | ⚠️ 조건부 허용 (개인정보 마스킹 후) |
| Level 3 (비밀) | 인증·암호화·핵심 비즈니스 로직, 크리덴셜, UTH 핵심기술 | ❌ 금지 (외부 클라우드 전체) |

> **Level 3 절대 금지 원칙**: Tier A(엔터프라이즈 계약) 도구를 포함하여 외부 클라우드 기반 AI 도구 전체에 Level 3(비밀) 데이터 입력은 금지된다. Level 3 데이터의 AI 활용이 필요한 경우 Tier S(사내 전용) 도구 도입을 AI 담당 부서에 요청한다.

---

### 2.2 활용 방식별 분류 및 특성

AI 코딩도구의 활용 방식은 세 가지로 구분하며, 자율성이 높을수록 더 엄격한 통제가 적용된다.

#### 웹 채팅 방식

브라우저 기반 대화형 인터페이스를 통해 코드 생성·분석·설계·문서화 등 개발 전 주기를 지원한다. 사용자가 직접 내용을 입력하고 결과를 확인하는 방식으로, AI 도구가 파일 시스템이나 외부 시스템에 자동으로 접근하지 않는다.

- **대표 도구**: Claude.ai, ChatGPT Business
- **주요 활용 사례**: 아키텍처 설계 리뷰, 코드 설명 요청, 리팩토링 방향 제안, 기술 문서 초안 작성, 단위 테스트 케이스 생성
- **핵심 통제 포인트**: 입력 데이터의 Level 준수 (→ 3장)

#### IDE 플러그인 방식

통합개발환경(IDE) 내에 통합되어 현재 열린 파일, 커서 위치, 선택 영역을 자동으로 인식하고 인라인 코드 완성 및 Agent 모드를 제공한다.

- **대표 도구**: JetBrains AI Pro, Cursor
- **주요 활용 사례**: 함수 자동완성, 오류 원인 설명, 유사 코드 탐색, Agent 모드를 통한 멀티파일 수정
- **핵심 통제 포인트**: IDE가 자동으로 참조하는 파일의 Level 확인. Agent 모드 사용 시 4장 적용

> **주의 - IDE 자동 참조**: IDE 플러그인은 현재 열려 있는 파일 전체를 컨텍스트로 AI에 전송할 수 있다. 작업 중인 파일에 크리덴셜(.env 파일 등) 또는 핵심 비즈니스 로직이 포함되어 있지 않은지 사전에 확인한다.

#### 코딩 에이전트 방식

프로젝트 전체 컨텍스트를 이해하고 파일 생성·수정·삭제, 터미널 명령 실행, 외부 API 호출 등을 자율적으로 수행하는 방식이다. 요령 제2조 5호의 "AI 에이전트"에 해당하며, 제42조의 권한 통제 규정이 직접 적용된다.

- **대표 도구**: Claude Code
- **주요 활용 사례**: 프로젝트 전체 리팩토링, 테스트 자동 생성 및 실행, 버그 탐지·수정·검증 루프, 병렬 작업 처리
- **핵심 통제 포인트**: 4장 전체(AI 에이전트 권한 통제), 특히 파일 삭제·네트워크 전송·외부 시스템 쓰기에 대한 사전 승인 의무

**방식별 통제 수준 비교**

| 구분 | AI 자율성 | 파일 시스템 접근 | 외부 전송 가능성 | 적용 장 |
|---|---|---|---|---|
| 웹 채팅 | 낮음 | 없음 | 입력 데이터만 | 3장 |
| IDE 플러그인 | 중간 | 열린 파일 자동 참조 | 열린 파일 컨텍스트 | 3장, 4장(Agent 모드) |
| 코딩 에이전트 | 높음 | 프로젝트 전체 | 설정에 따라 광범위 | 3장, 4장 전체, 5장 |

---

### 2.3 신규 도구 도입 및 변경 절차

「AI 도구 승인 목록」에 없는 도구를 업무에 새로 도입하거나, 기존 승인 도구의 플랜·계정 구조를 변경하려는 경우 아래 절차를 거쳐야 한다. (요령 제9조 ①②, 제15조)

```
① 도입 요청 부서
   └─ AI 도입 검토 요청서 작성 및 AX추진실 제출
       (도구명, 활용 목적, 예상 입력 데이터 Level, 사용 인원 포함)

② AX추진실 (AI 담당 부서)
   └─ 도구 Tier 판정 · 데이터 Level 검토 · 보안성 심의 의뢰
       → 30일 이내 요청 부서에 결과 회신 (요령 제9조 ②)

③ 인프라운영실
   └─ 「서비스보안성 심의 절차」에 따른 보안성 심의 수행 (요령 제15조 ①)

④ AI 거버넌스 위원회 (아래 사항 해당 시 필수 의결)
   └─ 심의 결과 검토 및 승인

⑤ 승인 도구 목록 등재 및 사용 개시
```

**위원회 의결이 필요한 경우** (요령 제9조 ③):

- 고영향 AI에 해당하는 도구·서비스의 도입
- 「AI 도구 활용 정책」 허용 기준의 예외를 인정하는 경우
- 회원사정보 또는 개인정보를 AI 학습·RAG에 사용하는 경우
- 대외 서비스에 AI 기능을 신규 추가하는 경우

**도입 요청서 제출처**: AX추진실 AI 헬프데스크 (ai-help@ktnet.co.kr)

---

### 2.4 미승인 도구 사용 금지

「AI 도구 승인 목록」에 등재되지 않은 AI 코딩도구는 사용 목적에 관계없이 업무에 사용할 수 없다. (요령 제15조 ①)

이 금지는 다음 각 호의 경우를 포함한다.

1. 회사 업무 관련 코드·데이터를 미승인 도구에 입력하는 행위
2. 개인 계정(무료 플랜 포함)으로 Tier A 도구와 동일한 서비스를 별도 이용하는 행위
3. IDE 마켓플레이스에서 검토 없이 AI 기능이 포함된 플러그인을 설치하여 업무에 사용하는 행위
4. 미승인 MCP 서버를 코딩 에이전트에 연결하여 업무에 활용하는 행위

새로운 도구를 도입하고자 하는 경우 2.3절의 도입 검토 요청 절차를 통해 사전 승인을 받아야 한다.

> **위반 시 조치**: 미승인 도구 사용으로 정보 유출이 발생한 경우 「인사관리규정」 제50조부터 제56조의 징계 절차가 적용될 수 있으며, 민·형사상 책임이 별도로 부과될 수 있다. (요령 제49조 ⑥)

---

*— 다음: 제3장 코드·데이터 입력 기준 —*

---

## 제3장 코드·데이터 입력 기준

> **근거 조항**: 「인공지능 활용 및 관리 요령」 제16조 (입력 금지 정보) ①6호·7호

이 장은 AI 코딩도구에 입력할 수 있는 코드와 데이터의 범위를 정한다. AI 코딩도구는 입력된 내용을 외부 서버로 전송하므로, 무엇을 입력하느냐가 정보보안의 핵심이다.

---

### 3.1 소스코드 민감도 판단 기준

모든 소스코드는 아래 기준에 따라 Level을 판단한 후 입력 여부를 결정한다. **판단이 모호한 경우 반드시 상위 Level로 분류**한다. (요령 제12조 말항)

#### Level 3 (비밀) — 외부 AI 도구 입력 전면 금지

다음 각 호에 해당하는 코드 및 정보는 Tier A를 포함한 모든 외부 클라우드 AI 도구에 입력을 금지한다.

| 유형 | 구체적 예시 |
|---|---|
| **인증·암호화 로직** | 로그인 처리, 세션 관리, 비밀번호 해싱, 토큰 발급·검증, 암호화·복호화 함수 |
| **핵심 비즈니스 로직** | UTH(uTradeHub) 수출입 신고 처리 로직, 통관 자동화 핵심 알고리즘, 무역금융 심사 로직 |
| **크리덴셜·접속 정보** | API 키, 비밀번호, DB 접속 문자열, JWT Secret, SSH 키, OAuth Client Secret |
| **개인정보 포함 코드** | 실제 고객 성명·주민번호·계좌번호·연락처가 하드코딩되거나 주석에 포함된 코드 |
| **보안 설정 파일** | `.env`, `application-prod.yml`, `secrets.yaml` 등 운영 환경 설정 파일 |
| **핵심 기술 정보** | UTH 등 핵심 시스템의 소스코드 전체, 미공개 알고리즘·특허 관련 구현체 |

#### Level 2 (대외비) — 조건부 허용 (개인정보 마스킹 후)

| 유형 | 구체적 예시 |
|---|---|
| **일반 업무 로직** | 비핵심 업무 프로세스 코드, 내부 배치 작업, 일반 데이터 변환 로직 |
| **내부 API 명세** | 대외 공개되지 않은 내부 REST API 스펙, 인터페이스 정의 |
| **시스템 구성 정보** | 서버 내부 IP, 네트워크 구성도, 미공개 서비스 아키텍처 |
| **업무 매뉴얼·지침 관련 코드** | 특정 업무 흐름을 구현한 코드로 외부 공개 시 경쟁 우위가 손상될 수 있는 것 |

> **조건부 허용 요건**: 개인정보가 포함된 경우 가명처리 또는 마스킹 후 입력. 단, 계약서 원본·미공개 재무 정보·법무·감사 관련 데이터가 포함된 경우 Level 2라도 입력 불가. (요령 제16조 ② 단서)

#### Level 1 (일반정보) — 허용

| 유형 | 구체적 예시 |
|---|---|
| **오픈소스 라이브러리 활용 코드** | Spring, MyBatis, React 등 공개 라이브러리 사용 코드 |
| **공개 예정 또는 공개된 코드** | 오픈소스로 배포 예정인 유틸리티, 공개 API 클라이언트 |
| **테스트·학습용 더미 코드** | 실제 데이터가 없는 순수 구조·패턴 학습용 코드 |
| **일반 알고리즘 구현** | 정렬, 탐색, 자료구조 등 도메인 무관 알고리즘 |

---

### 3.2 도구 Tier별 입력 허용 매트릭스

```
                  ┌──────────────┬──────────────┬──────────────┐
                  │  Tier A      │  Tier B      │  Tier S      │
                  │ (엔터프라이즈)│ (개인 계정)  │ (사내 전용) │
  ┌───────────────┼──────────────┼──────────────┼──────────────┤
  │ Level 1       │      ✅      │      ✅      │      ✅      │
  │ (일반정보)    │    허용      │    허용      │    허용      │
  ├───────────────┼──────────────┼──────────────┼──────────────┤
  │ Level 2       │   ⚠️ 조건부  │      ❌      │   ⚠️ 조건부  │
  │ (대외비)      │ 마스킹 후 가능│    금지     │  심의 후 가능│
  ├───────────────┼──────────────┼──────────────┼──────────────┤
  │ Level 3       │      ❌      │      ❌      │   ⚠️ 조건부  │
  │ (비밀)        │    금지      │    금지      │ 별도 심의 필수│
  └───────────────┴──────────────┴──────────────┴──────────────┘
```

> 현재 회사 AI 코딩도구는 모두 **Tier A**다. Tier B(개인 계정) 도구는 2.4절에 따라 업무 목적 사용이 금지된다. Tier S(사내 전용)는 별도 도입 심의가 필요하다.

---

### 3.3 절대 입력 금지 항목

아래 항목은 도구 등급에 관계없이 **모든 외부 클라우드 AI 코딩도구**에 입력이 금지된다. (요령 제16조 ① 6호·7호)

#### ① 소스코드 중 인증·암호화·핵심 비즈니스 로직 (제16조 ①6호)

다음 패턴의 코드가 포함된 파일·블록은 입력 불가:

```
- 로그인, 회원가입, 세션 처리 관련 서버 측 코드
- 비밀번호 해싱·검증 로직 (BCrypt, PBKDF2 등 구현 포함)
- JWT, OAuth, SAML 토큰 발급·검증 로직
- 대칭·비대칭 암호화 키 처리 코드 (AES, RSA 등 구현체)
- UTH 수출입 신고 처리 핵심 로직
- 통관 적격성 판단 알고리즘
- 무역금융 심사·승인 처리 로직
- HS 코드 자동 분류 핵심 알고리즘
```

#### ② 크리덴셜 및 접속 정보 (제16조 ①7호)

파일·주석·문자열 어디에 있든 아래 정보가 포함된 경우 입력 불가:

```
- API 키 (예: sk-ant-..., AIzaSy..., AKIA...)
- DB 접속 문자열 (jdbc:oracle://..., mongodb://user:password@...)
- 비밀번호, PIN 번호
- JWT Secret, HMAC Secret
- OAuth Client ID / Client Secret
- SSH 개인키 (-----BEGIN RSA PRIVATE KEY-----)
- 인증서 파일 내용 (.pem, .p12, .jks)
- 환경변수 파일 내용 (.env, application-prod.properties)
- 클라우드 액세스 키 (AWS, Azure, GCP)
```

---

### 3.4 안전한 코드 입력을 위한 전처리 방법

금지 항목을 제거·치환하면 Level 3 코드도 일부 AI 활용이 가능해진다. 아래 방법을 사용한다.

#### 방법 1 — 크리덴셜 플레이스홀더 치환

AI에 입력하기 전, 실제 값을 의미 있는 플레이스홀더로 교체한다.

**❌ 입력 금지 (원본)**
```java
DataSource dataSource() {
    config.setJdbcUrl("jdbc:oracle:thin:@10.10.5.21:1521:UTRADEDB");
    config.setUsername("utrack_app");
    config.setPassword("P@ssw0rd!2025");
    config.addDataSourceProperty("ssl", "true");
}
```

**✅ 입력 가능 (전처리 후)**
```java
DataSource dataSource() {
    config.setJdbcUrl(System.getenv("DB_URL"));        // 플레이스홀더
    config.setUsername(System.getenv("DB_USERNAME"));  // 플레이스홀더
    config.setPassword(System.getenv("DB_PASSWORD"));  // 플레이스홀더
    config.addDataSourceProperty("ssl", "true");
    // 질문: HikariCP 커넥션 풀 설정에서 타임아웃 최적화 방법을 알려주세요.
}
```

#### 방법 2 — 핵심 비즈니스 로직 추상화

구체적인 도메인 로직을 노출하지 않고 패턴·구조만 질문한다.

**❌ 입력 금지 (핵심 로직 노출)**
```java
// UTH 수출입 신고 적격성 검증 로직 전체를 붙여넣고 질문
public boolean validateExportDeclaration(ExportDecl decl) {
    if (decl.getHsCode().startsWith("8471") && decl.getDestCountry().equals("CN")) {
        return checkStrategicGoodsList(decl); // 전략물자 검증
    }
    // ... (핵심 통관 비즈니스 로직)
}
```

**✅ 입력 가능 (추상화 후)**
```java
// 아래 패턴에서 if-else 중첩을 줄이고 전략 패턴으로 리팩토링하는 방법을 알려주세요.
public boolean validateItem(Item item) {
    if (item.getCategory().equals("A") && item.getTarget().equals("X")) {
        return checkSpecialList(item);
    }
    if (item.getValue() > THRESHOLD) {
        return checkHighValueRule(item);
    }
    return true;
}
```

#### 방법 3 — 개인정보 마스킹 (Level 2 조건부 허용 요건)

테스트 코드 또는 데이터 처리 로직에 포함된 개인정보를 가명처리한다.

**❌ 입력 금지 (실제 개인정보 포함)**
```java
// 단위 테스트에 실제 고객 데이터 사용
User user = new User("홍길동", "880101-1234567", "010-1234-5678");
```

**✅ 입력 가능 (가명처리 후)**
```java
// 더미 데이터 또는 가명처리된 테스트 데이터 사용
User user = new User("테스트사용자A", "000101-0000000", "010-0000-0000");
```

#### 방법 4 — 입력 전 체크리스트 (30초 점검)

코드를 AI 도구에 붙여넣기 전, 아래 항목을 확인한다.

```
□ 1. 파일에 .env, application-prod.* 내용이 포함되어 있지 않은가?
□ 2. 비밀번호, API 키, 토큰 등 크리덴셜 문자열이 없는가?
□ 3. 로그인·인증·암호화 핵심 로직이 포함되어 있지 않은가?
□ 4. UTH 등 핵심 시스템의 비즈니스 로직이 포함되어 있지 않은가?
□ 5. 실제 고객·직원의 개인정보(성명, 주민번호, 계좌 등)가 없는가?
□ 6. DB 접속 문자열, 서버 내부 IP가 하드코딩되어 있지 않은가?
```

4개 이상 해당 항목이 있으면 입력 전에 전처리가 필요하다. 판단이 어려운 경우 AI 헬프데스크(ai-help@ktnet.co.kr)에 문의한다.

---

### 3.5 위반 시 즉시 조치

입력 금지 정보를 AI 도구에 실수로 입력한 경우, 임직원은 **즉시** 아래 절차에 따라 보고하여야 한다. (요령 제16조 ④, 제50조 ①)

```
발생 인지 즉시
  └─ 1. AI 도구 화면에서 해당 대화(세션) 즉시 삭제 (가능한 경우)
  └─ 2. 입력한 내용, 도구명, 발생 시각을 메모

1시간 이내
  └─ 직속 부서장 및 AX추진실 사고대응 담당자에게
     전화 또는 메신저로 1차 보고

24시간 이내
  └─ 사고 보고서 작성·제출 (양식: 「정보보호관리세칙」 소정 양식)

이후 절차
  └─ 개인정보 포함 시: 72시간 이내 감독기관 신고 (요령 제50조 ①3호)
  └─ 재발방지대책: 7일 이내 수립 및 감사팀 제출
```

> **자진 보고 감경**: 규정 위반이더라도 즉시 자진 보고하고 후속 조치에 성실히 협력한 경우, 징계 양정에 감경 사유로 반영될 수 있다. 발견이 두려워 은폐하는 행위가 더 중한 처분을 초래한다.

---

*— 다음: 제4장 AI 에이전트(Claude Code) 사용 규칙 —*

---

## 제4장 AI 에이전트(Claude Code) 사용 규칙

> **근거 조항**: 「인공지능 활용 및 관리 요령」 제42조 (AI 에이전트 권한 통제)

이 장은 코딩 에이전트, 특히 **Claude Code**의 안전한 사용 기준을 정한다. 코딩 에이전트는 사용자의 지시를 받아 파일 시스템, 터미널, 외부 API를 자율적으로 조작하므로, 일반 AI 도구보다 높은 수준의 통제가 요구된다.

---

### 4.1 AI 에이전트 정의 및 적용 범위

요령 제2조 5호는 **AI 에이전트**를 "사용자의 지시를 받아 외부 시스템·도구를 자율적으로 호출하여 일련의 작업을 수행하는 AI"로 정의한다. Claude Code는 다음 특성으로 인해 이 정의에 해당하며, 요령 제42조의 권한 통제 규정이 직접 적용된다.

| 특성 | 내용 | 통제 필요 이유 |
|---|---|---|
| 파일 시스템 접근 | 프로젝트 디렉토리 내 파일 읽기·생성·수정·삭제 | 실수·오작동 시 코드 손실 발생 |
| 터미널 명령 실행 | 빌드, 테스트, 배포 스크립트 자율 실행 | 의도치 않은 시스템 변경 가능 |
| 외부 네트워크 호출 | `git push`, 외부 API 호출, 패키지 설치 | 민감정보 외부 전송 가능성 |
| MCP 서버 연동 | 외부 도구(DB, Slack, Jira 등)와 연결 가능 | 사내 시스템 무단 접근 가능성 |
| 병렬·연속 작업 | 계획→실행→테스트→수정 루프 자율 반복 | 오류가 광범위하게 전파될 수 있음 |

**IDE Agent 모드와의 차이**: JetBrains AI Agent, Cursor Agent 등 IDE 내장 Agent 모드도 에이전트 특성을 가지나, 일반적으로 프로젝트 범위 내 파일 수정에 한정된다. Claude Code처럼 터미널 전체와 외부 네트워크에 자율적으로 접근하는 경우에는 이 장의 기준을 동일하게 적용한다.

---

### 4.2 작업 유형별 실행 통제 기준

Claude Code의 작업은 **자율 실행 허용**과 **사전 승인 필수**로 구분한다.

#### 자율 실행 허용 범위

아래 작업은 사용자의 별도 확인 없이 Claude Code가 자율적으로 수행할 수 있다.

| 작업 유형 | 구체적 예시 |
|---|---|
| 파일·코드 읽기 | 소스코드 분석, 로그 파일 읽기, 디렉토리 구조 탐색 |
| 새 파일·코드 생성 | 신규 클래스 생성, 단위 테스트 파일 작성, 설정 파일 초안 작성 |
| 빌드·컴파일 | `./gradlew build`, `mvn compile`, `npm run build` |
| 단위·통합 테스트 실행 | `./gradlew test`, `pytest`, `npm test` |
| Git 읽기 작업 | `git status`, `git log`, `git diff` |
| 패키지 의존성 조회 | `gradle dependencies`, `npm list` |
| 코드 정적 분석 | 린터 실행, 코드 품질 검사 도구 실행 |

#### 사전 승인 필수 작업 (요령 제42조 ①)

아래 5가지 범주에 해당하는 작업은 Claude Code가 **실행 직전 반드시 사용자의 명시적 확인**을 받아야 한다. Claude Code는 기본적으로 실행 전 확인을 요청하는 동작을 하나, 자동 승인 설정이 되어 있다면 즉시 해제해야 한다.

| 범주 | 해당 작업 예시 | 위험 내용 |
|---|---|---|
| **① 파일 영구 변경·삭제** | `rm`, 파일 덮어쓰기, DB 스키마 변경 스크립트 실행 | 복구 불가능한 코드·데이터 손실 |
| **② 외부 네트워크 데이터 전송** | `git push`, 외부 API에 데이터 POST, FTP 업로드 | 미검증 코드 또는 민감정보 외부 유출 |
| **③ 사내 시스템 쓰기 작업** | ERP, HR, 형상관리 시스템 데이터 변경, DB INSERT/UPDATE/DELETE | 운영 데이터 무결성 훼손 |
| **④ 대외 통신 발송** | 이메일·문자·메신저 발송 API 호출, 알림 트리거 | 사전 검토 없는 외부 통신 발송 |
| **⑤ 결제·계약 행위** | 유료 API 과금 호출, 서비스 가입·구독 API 실행 | 예산 외 비용 발생 |

> **실행 확인 방법**: Claude Code는 위 작업 직전 실행 계획을 텍스트로 표시하고 확인을 요청한다. 내용을 반드시 읽고 의도한 작업인지 확인한 후 승인한다. 내용이 불명확하거나 예상 범위를 벗어났다면 거부하고 작업 범위를 재지정한다.

---

### 4.3 안전한 에이전트 환경 구성

Claude Code의 행동을 사전에 제약하는 두 가지 핵심 도구를 활용한다.

#### CLAUDE.md — 프로젝트별 작업 범위 명시

`CLAUDE.md`는 Claude Code가 프로젝트 시작 시 자동으로 로드하는 지시 파일이다. 프로젝트 루트에 작성하면 작업 범위·금지 사항·코딩 규칙을 에이전트에 사전 제약할 수 있다. 아래 템플릿을 기반으로 프로젝트에 맞게 수정하여 사용한다.

```markdown
# CLAUDE.md — [프로젝트명] Claude Code 운영 지침
# 최종 수정: YYYY-MM-DD | 담당: [담당자명]

## 1. 허용 작업 범위
- 이 프로젝트의 src/ 하위 파일 읽기·수정·생성
- 단위 테스트(JUnit) 작성 및 실행: ./gradlew test
- 빌드 및 컴파일: ./gradlew build
- Git 상태 조회(읽기 전용): git status, git log, git diff

## 2. 절대 금지 작업 (확인 요청 없이 실행 불가)
- git push, git merge 등 원격 저장소 변경 작업
- rm, 파일 영구 삭제 명령
- DB 직접 접속 및 DML(INSERT/UPDATE/DELETE) 실행
- 외부 API 호출 (특히 POST/PUT/DELETE 메서드)
- 환경 변수 파일(.env, application-prod.*) 읽기 또는 수정
- src/main/java/ktnet/auth/ 하위 인증 모듈 코드 수정

## 3. 코딩 규칙
- Java 17 문법 사용
- 패키지 구조: com.ktnet.[모듈명].[계층명]
- 예외 처리: KtnetException 상속 사용
- 로깅: SLF4J + Logback, 개인정보 로그 출력 금지

## 4. 테스트 데이터 규칙
- 테스트에 실제 고객·회원사 데이터 사용 금지
- 더미 데이터 사용: TestDataFactory 클래스 활용

## 5. 민감 경로 (절대 읽기·수정 금지)
- src/main/resources/application-prod.*
- .env, .env.*
- src/main/java/ktnet/auth/
- src/main/java/ktnet/crypto/
```

> **팁**: `CLAUDE.md`는 프로젝트의 보안 경계를 정의하는 문서다. 팀 전체가 공유하는 Git 저장소에 커밋하여 모든 구성원이 동일한 제약 하에 Claude Code를 사용하도록 한다.

#### Hooks — 위험 작업 자동 차단

Claude Code의 Hooks 기능을 사용하면 특정 도구 호출 전후에 자동으로 검증 스크립트를 실행할 수 있다. 아래 설정을 `.claude/settings.json`에 추가하면 위험 명령 실행 시 자동으로 차단하거나 경고를 표시한다.

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash /home/claude/hooks/pre-bash-check.sh"
          }
        ]
      }
    ]
  }
}
```

**`pre-bash-check.sh` 예시 — 위험 명령 사전 차단**

```bash
#!/bin/bash
# Claude Code Bash 명령 실행 전 위험도 검사
# 표준 입력으로 실행 예정 명령어를 받음
COMMAND=$(cat)

# 금지 패턴 목록
FORBIDDEN_PATTERNS=(
    "git push"
    "git merge"
    "git rebase"
    "rm -rf"
    "DROP TABLE"
    "DELETE FROM"
    "application-prod"
    "\.env"
)

for pattern in "${FORBIDDEN_PATTERNS[@]}"; do
    if echo "$COMMAND" | grep -qi "$pattern"; then
        echo "[KTNET 보안 Hook] 위험 명령 감지: '$pattern' 포함" >&2
        echo "이 작업은 사람의 명시적 승인이 필요합니다. (요령 제42조)" >&2
        exit 1  # 실행 차단
    fi
done

exit 0  # 정상 실행 허용
```

---

### 4.4 최소 권한 원칙 및 정기 권한 검토

#### 최소 권한 원칙 적용 (요령 제42조 ③)

Claude Code를 사용하는 환경에서 에이전트에 부여된 권한은 작업에 필요한 최소 범위로 제한한다. 구체적으로 아래 사항을 준수한다.

- **작업 디렉토리 제한**: 에이전트가 접근할 수 있는 디렉토리를 현재 프로젝트 범위로 한정한다. 다른 프로젝트 디렉토리, 시스템 디렉토리(`/etc`, `/usr` 등)에 접근할 이유가 없는 작업에서는 명시적으로 범위를 제한한다.
- **운영 환경 자격증명 분리**: 로컬 개발 환경에서는 운영 DB·API 자격증명이 설정 파일에 존재하지 않도록 한다. Claude Code가 접근 가능한 `.env` 파일에는 개발·테스트 환경 자격증명만 존재해야 한다.
- **MCP 서버 최소 권한**: 연결된 MCP 서버에는 해당 작업에 필요한 최소 권한만 부여한다. DB 조회만 필요한 작업에 쓰기 권한을 가진 DB MCP 서버를 연결하지 않는다.

#### 분기 1회 권한 검토 (요령 제42조 ③)

각 부서 개발 리더는 분기 1회 아래 항목을 점검하여 AX추진실에 결과를 제출한다.

```
□ 팀 내 CLAUDE.md가 최신 상태로 유지되고 있는가?
□ Hooks 스크립트가 정상 동작하는가?
□ 연결된 MCP 서버 목록이 현재 업무에 필요한 범위인가?
□ 팀원 중 자동 승인(auto-approve) 설정을 활성화한 사람은 없는가?
□ Claude Code 사용 로그에 이상 작업 패턴이 없는가?
```

---

### 4.5 비용 관리 및 세션 운영

Claude Code는 사용량에 따라 비용이 발생하며, 통제 워크스페이스를 통해 조직 전체 비용을 관리한다.

#### 지출 한도 설정 (관리자)

Anthropic Console에서 조직 전체 한도와 개인별 한도를 계층적으로 설정한다.

| 관리 항목 | 설정 위치 | 담당 |
|---|---|---|
| 조직 월간 지출 한도 | Console → Organization Settings → Usage | AX추진실 (Owner) |
| 개인별 월간 지출 한도 | Console → 개인 설정 | AX추진실 (Owner) |
| Extra Usage 차단 | 현행 정책 유지 — 초과 시 다음 주기까지 대기 | 전 사용자 |

#### 세션 비용 최적화 (개인)

| 명령어 | 효과 | 권장 사용 시점 |
|---|---|---|
| `/clear` | 대화 이력 초기화, 새 컨텍스트 시작 | 독립적인 새 작업 시작 시 |
| `/compact` | 대화 이력을 요약하여 토큰 절감 | 동일 작업이 길어질 때 |
| `/model` | Sonnet ↔ Opus 전환 | 단순 작업은 Sonnet, 복잡한 설계는 Opus |
| `/cost` | 현재 세션 누적 비용 확인 | 작업 전후 비용 모니터링 |

**효율적인 요청 방식**

- 여러 번의 소규모 수정 요청보다 **한 번의 종합적인 요청**이 토큰 사용 면에서 효율적이다.
- 새 작업 시작 시 `/clear`로 이전 대화를 초기화하면 불필요한 컨텍스트 토큰을 절감할 수 있다.
- 단순 코드 완성·수정 작업에는 IDE 플러그인(JetBrains AI, Cursor) 우선 활용을 권장한다. Claude Code는 멀티파일 리팩토링, 프로젝트 전체 탐색, 테스트 자동화 등 에이전트 특성이 필요한 작업에 집중하여 사용한다.

---

### 4.6 금지 사용 사례

아래 사례는 요령 제42조 위반에 해당한다.

| 사례 | 위반 내용 |
|---|---|
| 운영 DB에 직접 접속하는 MCP 서버를 연결한 채 에이전트를 실행 | 사내 시스템 쓰기 작업 무단 실행 가능성 (①5호) |
| `git push origin main`을 자동 승인으로 설정 | 외부 네트워크 데이터 전송 사전 승인 미이행 (①4호) |
| 배포 스크립트 실행을 에이전트에 위임하고 확인 없이 방치 | 영구적 시스템 변경 사전 승인 미이행 (①3호) |
| 운영 환경 `.env` 파일이 있는 디렉토리에서 Claude Code 실행 | Level 3 데이터 에이전트 접근 허용 (3장 위반 병행) |
| CLAUDE.md 없이 프로젝트 전체를 에이전트에 무제한 개방 | 최소 권한 원칙 미적용 (제42조 ③) |

---

*— 다음: 제5장 MCP 서버·IDE 플러그인 보안 관리 —*

---

## 제5장 MCP 서버·IDE 플러그인 보안 관리

> **근거 조항**: 「인공지능 활용 및 관리 요령」 제43조 (공급망 보안 및 AI-BOM)

이 장은 AI 코딩도구의 기능을 확장하는 외부 구성 요소—MCP 서버, IDE 플러그인, 벡터 데이터베이스 등—의 도입 및 관리 기준을 정한다. 요령 제43조는 이들을 공급망으로 규정하고 도입 전 보안 검토, AI-BOM 등재, 취약점 대응 기한 준수를 의무화한다.

---

### 5.1 MCP 서버의 보안 위험 특성

MCP(Model Context Protocol) 서버는 Claude Code 등 코딩 에이전트가 외부 도구·데이터에 접근할 수 있도록 연결하는 중간 서버다. 편의성이 높은 반면, 아래와 같은 고유한 보안 위험을 가진다.

| 위험 유형 | 구체적 위협 | 실제 발생 시나리오 |
|---|---|---|
| **임의 코드 실행** | 악성 MCP 서버가 에이전트를 통해 임의 명령 실행 | 오픈소스 커뮤니티 MCP 서버에 악성 코드 삽입 |
| **데이터 탈취** | 에이전트가 접근한 파일·DB 내용을 외부로 유출 | 내부 DB에 접속하는 MCP 서버가 데이터를 외부 서버로 전송 |
| **프롬프트 인젝션** | MCP 서버가 반환하는 데이터에 악의적 지시문 삽입 | Jira 이슈 내용에 숨겨진 지시문이 에이전트 동작 조작 |
| **권한 상승** | 필요 이상의 권한을 가진 MCP 서버를 통한 무단 접근 | DB 조회용 MCP 서버가 실제로는 DDL 권한 보유 |
| **공급망 오염** | 신뢰할 수 없는 출처의 MCP 서버 설치 | npm·pypi에 게시된 악성 MCP 패키지 설치 |

> 요령 제41조는 프롬프트 인젝션을 포함한 LLM 공격 위협에 대해 OWASP LLM Top 10 기준의 방어 조치를 요구한다. MCP 서버 도입 시 이 위협 목록을 함께 검토한다.

---

### 5.2 MCP 서버 도입 전 보안 검토 절차

요령 제43조 ①에 따라 MCP 서버를 새로 도입하거나 기존 서버를 업그레이드하는 경우, 아래 4개 항목을 사전에 검토하고 결과를 AI-BOM에 기록해야 한다.

#### 필수 검토 4개 항목 (요령 제43조 ① 1~4호)

**① 라이선스·출처·공급자 보안 인증**
- 라이선스 유형 확인 (MIT, Apache 2.0 등 상업 이용 허용 여부)
- 공급자(개발사) 공식 여부 확인 — 비공식 커뮤니티 서버는 원칙적으로 금지
- 공급자의 보안 인증 보유 여부 (SOC 2, ISO 27001 등)
- GitHub 저장소가 있는 경우: 최근 커밋 이력, 이슈 대응 활성도, 스타 수

**② 알려진 취약점(CVE) 및 보안 권고문**
- NIST NVD(https://nvd.nist.gov), GitHub Security Advisories에서 CVE 검색
- 패키지명으로 `npm audit`, `pip audit` 실행 결과 확인
- 사용 의존 라이브러리의 알려진 취약점 포함 여부

**③ 업데이트 이력 및 유지보수 상태**
- 최근 6개월 내 업데이트 여부 — 장기 미업데이트 서버는 보안 패치 누락 가능성
- 보안 이슈 발생 시 패치 배포까지 소요 시간 (이력으로 판단)
- End-of-Life(EOL) 여부 또는 EOL 예정 일정 확인

**④ 데이터 처리 방식 및 학습 이용 여부**
- MCP 서버가 에이전트로부터 수신한 데이터를 외부 서버로 전송하는지 여부
- 데이터를 서비스 제공사의 학습에 이용하는지 여부
- 데이터 저장 여부 및 저장 위치 (국내·외 서버)

#### 도입 검토 체크리스트

```
[ MCP 서버 보안 검토 체크리스트 ]
서버명: ___________________  버전: _______  검토일: ________  검토자: ________

① 라이선스·출처
  □ 라이선스 유형 확인 및 상업 이용 가능 여부: ____________
  □ 공식 공급자(Anthropic, JetBrains, 검증된 기업) 배포 여부:  □ 예  □ 아니오
  □ 공급자 보안 인증 보유:  □ 있음 (_______)  □ 없음

② 취약점 검사
  □ CVE 검색 결과:  □ 없음  □ 있음 (심각도: _________, 패치 여부: ________)
  □ npm audit / pip audit 결과:  □ 이상 없음  □ 취약점 있음 (_______)

③ 유지보수 상태
  □ 최근 업데이트:  _________ (6개월 이내:  □ 예  □ 아니오)
  □ EOL 여부 또는 예정:  □ 해당 없음  □ EOL 예정 (_______)

④ 데이터 처리 방식
  □ 외부 서버 데이터 전송 여부:  □ 없음  □ 있음 (목적: _____________)
  □ 학습 데이터 이용 여부:  □ 없음  □ 있음
  □ 데이터 저장 위치:  □ 로컬 전용  □ 외부 서버 (_____________)

최종 판정:  □ 도입 승인  □ 조건부 승인 (조건: _______)  □ 도입 불가
승인자(AX추진실): ________________________
```

---

### 5.3 승인된 MCP 서버 목록 및 신청 절차

#### 사전 승인된 MCP 서버

아래 목록은 AX추진실이 보안 검토를 완료한 서버다. 최신 목록은 「AI 도구 승인 목록」(AI-LIST-001)에서 확인한다.

| MCP 서버 | 공급자 | 허용 용도 | 허용 권한 범위 |
|---|---|---|---|
| `filesystem` (공식) | Anthropic | 프로젝트 내 파일 읽기·쓰기 | 지정 디렉토리 한정, 삭제 금지 |
| `git` (공식) | Anthropic | Git 이력 조회 | 읽기 전용 (push 금지) |
| `fetch` (공식) | Anthropic | 공개 URL 콘텐츠 조회 | GET 요청만 허용 |
| GitLab MCP | GitLab | 사내 GitLab 이슈·MR 조회 | 읽기 전용 |

> **목록 외 MCP 서버**: 위 목록에 없는 MCP 서버는 사용 금지이며, 신규 도입이 필요한 경우 아래 절차로 신청한다.

#### 신규 MCP 서버 도입 신청 절차

```
① 신청자
   └─ AX추진실에 이메일 신청 (ai-help@ktnet.co.kr)
      포함 내용: 서버명, 버전, 공급자, 활용 목적, 접근 데이터 Level,
                위 체크리스트 자체 검토 결과

② AX추진실
   └─ 보안 검토 수행 (5.2절 4개 항목)
   └─ 인프라운영실 협의 (필요 시)
   └─ 14영업일 이내 검토 결과 회신

③ 승인 시
   └─ 「AI 도구 승인 목록」 등재
   └─ AI-BOM에 신규 항목 추가
```

---

### 5.4 IDE 플러그인 보안 관리

IDE 마켓플레이스(JetBrains Marketplace, VSCode Extension Marketplace 등)에서 설치하는 AI 관련 플러그인도 요령 제43조의 공급망 보안 대상이다.

#### 설치 전 확인 사항

| 확인 항목 | 기준 |
|---|---|
| **공급자** | 공식 벤더(JetBrains, GitHub, Anthropic 등) 또는 신뢰할 수 있는 기업 배포 플러그인인가? |
| **다운로드 수·평점** | JetBrains Marketplace 기준 다운로드 1만 건 이상, 평점 3.5 이상 권장 |
| **최근 업데이트** | 6개월 이내 업데이트 여부 |
| **요청 권한** | 네트워크 접근·파일 시스템 접근 권한 요청 범위가 기능과 합리적으로 일치하는가? |
| **데이터 수집** | 개인정보처리방침에서 코드·입력 데이터 수집·외부 전송 여부 확인 |

#### 설치 금지 유형

- 검토 없이 npm·pypi·GitHub에서 직접 내려받아 설치하는 비공식 AI 플러그인
- 공급자 신원이 불분명한 마켓플레이스 AI 어시스턴트 플러그인
- AI 코딩 기능이 있으나 데이터 처리 방침이 명시되어 있지 않은 플러그인
- `요령 AI 도구 승인 목록`에 없는 AI 서비스에 연결되는 플러그인

---

### 5.5 AI-BOM 관리

요령 제43조 ②에 따라 운영 환경에서 사용하는 AI 코딩도구 구성 요소 전체에 대해 AI-BOM을 작성·관리한다.

#### AI-BOM 등재 대상 (코딩도구 관련)

- 사용 중인 AI 코딩도구 본체 (Claude.ai, JetBrains AI Pro, Cursor 등)
- 연결된 MCP 서버 전체
- AI 기능이 포함된 IDE 플러그인
- 코딩 에이전트가 호출하는 외부 API (라이브러리 레지스트리, 문서 API 등)

#### AI-BOM 항목 구성

각 구성 요소별로 아래 항목을 기록한다.

```
구성 요소명    : Claude.ai (Claude Code 포함)
유형          : AI 도구 (웹 채팅·코딩 에이전트)
공급자        : Anthropic PBC
현재 버전     : Claude Sonnet 4.6 (2026.06 기준)
라이선스      : 서비스 이용 약관 (Team Plan)
계약 유형     : Tier A / ZDR (Zero Data Retention) 적용
도입 승인일   : 2026-01-15
최근 보안 검토: 2026-06-01
알려진 CVE    : 해당 없음
데이터 전송   : 입력 데이터 → Anthropic 서버 (ZDR 계약, 학습 미사용)
EOL 예정      : 없음 (지속 업데이트)
담당자        : AX추진실 홍길동
비고          : 통제 워크스페이스 운영 중
```

#### 유지 관리 주기 및 의무

| 활동 | 주기 | 담당 |
|---|---|---|
| AI-BOM 전체 갱신 스캐닝 | **분기 1회** | AX추진실 |
| 신규 구성 요소 등재 | 도입 즉시 | AX추진실 |
| CVE 신규 발생 시 업데이트 | 인지 즉시 | AX추진실 |
| AI-BOM 변경 이력 보관 | **5년** | AX추진실 |

---

### 5.6 취약점 대응 기한

AI-BOM에 등재된 구성 요소에서 취약점이 발견된 경우 아래 기한 내에 조치한다. (요령 제43조 ③④)

```
┌─────────────────────────────────────────────────────────────┐
│  심각도 분류별 대응 기한                                    │
├──────────────────┬──────────────────┬──────────────────────┤
│  심각(Critical)  │  인지일로부터    │  72시간 이내         │
│  알려진 제로데이  │                 │  임시조치(차단·격리) │
│                  │                 │  + 30일 이내 패치    │
├──────────────────┼──────────────────┼──────────────────────┤
│  높음(High)      │  인지일로부터    │  30일 이내 대응      │
├──────────────────┼──────────────────┼──────────────────────┤
│  중간(Medium)    │  인지일로부터    │  90일 이내 대응      │
├──────────────────┼──────────────────┼──────────────────────┤
│  낮음(Low)       │  다음 정기 점검  │  (차기 분기 점검 반영)│
└──────────────────┴──────────────────┴──────────────────────┘
```

**EOL(End-of-Life) 구성 요소**: 지원이 종료된 모델·라이브러리·플러그인은 EOL 공지일로부터 **90일 이내**에 지원 가능한 대체재로 이전한다. (요령 제43조 ④)

**치명적 취약점 발견 시 즉시 조치 흐름**

```
취약점 인지
  └─ 즉시: 해당 MCP 서버·플러그인 비활성화 (차단·격리)
  └─ 4시간 이내: AX추진실에 취약점 내용 보고
  └─ 24시간 이내: 임시 조치 완료 및 보고
  └─ 30일 이내: 패치 버전 적용 또는 대체재 이전 완료
```

---

### 5.7 금지된 MCP 서버·플러그인 유형

다음 유형은 보안 검토 없이 업무 환경에서 사용할 수 없다.

| 유형 | 금지 이유 |
|---|---|
| 개인이 npm·pip·GitHub에서 직접 내려받은 비공식 MCP 서버 | 출처 불명, 악성 코드 포함 가능성 |
| 운영 DB에 DML(쓰기) 권한으로 접속하는 MCP 서버 | 사내 시스템 무단 쓰기 위험 (요령 제42조 ①5호) |
| 에이전트 입력 데이터를 외부 서버로 전송하는 MCP 서버 | 민감정보 외부 유출 위험 (요령 제16조) |
| Slack·이메일 등 대외 통신을 쓰기 권한으로 연결하는 서버 | 대외 통신 무단 발송 위험 (요령 제42조 ①2호) |
| AI 도구 승인 목록에 없는 AI 서비스에 연결하는 MCP 서버 | 미승인 도구 우회 사용에 해당 (요령 제15조) |
| 최근 6개월 이상 업데이트가 없는 커뮤니티 MCP 서버 | 보안 패치 미적용 상태 |

---

*— 다음: 제6장 AI 생성 코드 품질·저작권 관리 —*

---

## 제6장 AI 생성 코드 품질·저작권 관리

> **근거 조항**: 「인공지능 활용 및 관리 요령」 제18조 (결과물 검증), 제19조 (AI 생성물 표시), 제20조 (저작권 및 결과물 귀속)

이 장은 AI 코딩도구가 생성한 코드를 업무에 적용하기 전 반드시 수행해야 하는 검증 의무와, 생성된 코드의 저작권 귀속 및 라이선스 준수 기준을 정한다.

---

### 6.1 AI 생성 코드 검증 의무

요령 제18조 ①은 "AI 결과물을 업무에 사용하는 임직원은 그 결과물의 정확성, 출처, 편향 가능성을 스스로 검증한 후 사용하여야 한다"고 규정한다. AI 코딩도구가 생성한 코드도 이 의무의 대상이다.

#### 책임 원칙

- AI가 생성한 코드를 검증 없이 운영 환경에 배포하여 장애·보안 사고가 발생한 경우, **해당 임직원의 책임**으로 귀결된다. (요령 제18조 ②)
- 단, **규정을 준수하고 합리적인 검증을 거친 경우**에는 면책 사유가 적용될 수 있다. 이 장에서 정한 검증 절차를 이행했음을 기록으로 남기는 것이 중요하다.
- AI가 제안한 라이브러리 버전, 보안 권고, 법조문·규격 번호는 반드시 원문을 확인한 후 적용한다. (요령 제18조 ③)

#### 검증 3단계

AI 생성 코드를 운영에 적용하기 전 아래 세 단계를 순서대로 이행한다.

```
① 기능 정확성 검증
   └─ 코드가 요구사항을 올바르게 구현하는가?
   └─ 경계값·예외 케이스가 누락되지 않았는가?
   └─ 단위 테스트 통과 여부 확인

② 보안 취약점 검증
   └─ 알려진 보안 취약점 패턴이 포함되어 있는가?
   └─ 정적 분석 도구 실행 결과 확인

③ 라이선스 확인
   └─ AI가 제안한 라이브러리의 라이선스가 상업적 이용에 적합한가?
   └─ 전염성 라이선스(GPL 등)가 포함되어 있지 않은가?
```

---

### 6.2 보안 취약점 검증

AI 코딩도구는 기능적으로 동작하는 코드를 생성하더라도 보안 취약점이 포함된 코드를 반환하는 경우가 있다. 아래 취약점 유형을 중심으로 검토한다.

#### AI 생성 코드에서 자주 발견되는 취약점 패턴

| 취약점 유형 | 발생 패턴 예시 | 확인 방법 |
|---|---|---|
| **SQL 인젝션** | `"SELECT * FROM users WHERE id = " + userId` | Prepared Statement 사용 여부 확인 |
| **XSS (크로스사이트 스크립팅)** | 사용자 입력을 HTML에 직접 출력 | 출력 인코딩 처리 여부 확인 |
| **하드코딩 크리덴셜** | 코드 내 비밀번호·API 키 직접 삽입 | grep 또는 정적 분석 도구로 탐지 |
| **취약한 라이브러리 버전** | 보안 패치가 적용되지 않은 이전 버전 지정 | `npm audit`, `./gradlew dependencyCheckAnalyze` |
| **입력 값 미검증** | 사용자 입력 길이·형식 검증 없이 처리 | 입력 처리 경로 전체 추적 |
| **경로 순회 (Path Traversal)** | `../` 패턴을 활용한 상위 디렉토리 접근 가능 | 파일 경로 처리 코드 검토 |
| **정보 노출** | 예외 발생 시 스택 트레이스·내부 정보 외부 노출 | 예외 처리 응답 내용 확인 |
| **취약한 난수 생성** | 보안에 `Math.random()` 사용 | `SecureRandom` 사용 여부 확인 |

#### 정적 분석 도구 활용

AI 생성 코드를 커밋 전 정적 분석 도구를 통해 검증한다. 사내 CI/CD 파이프라인에 연동되어 있는 경우 자동으로 수행되나, 로컬에서 먼저 확인하는 습관이 권장된다.

| 대상 | 도구 | 실행 명령 |
|---|---|---|
| Java | SpotBugs, SonarLint | IDE 플러그인 또는 `./gradlew spotbugsMain` |
| JavaScript/TypeScript | ESLint (security 플러그인) | `npx eslint --ext .js,.ts src/` |
| Python | Bandit | `bandit -r src/` |
| 의존성 취약점 (전체) | OWASP Dependency-Check | `./gradlew dependencyCheckAnalyze` |
| 크리덴셜 탐지 | git-secrets, truffleHog | `git-secrets --scan` |

> **AI 제안 라이브러리 버전 확인**: AI 코딩도구는 학습 데이터 기준 시점의 라이브러리 버전을 제안하는 경우가 있다. 제안된 버전이 현재 최신 안정 버전인지, 해당 버전에 알려진 CVE가 없는지 반드시 확인한다.

---

### 6.3 오픈소스 라이선스 확인

AI 코딩도구가 제안하는 코드에는 오픈소스 라이브러리를 활용하거나, 오픈소스 코드 패턴을 포함하는 경우가 있다. 라이선스 위반은 법적 리스크로 이어지므로, 신규 라이브러리 도입 시 아래 기준으로 확인한다. (요령 제20조 ③)

#### 라이선스 유형별 업무 적용 기준

| 라이선스 | 상업적 이용 | 소스 공개 의무 | KTNET 업무 적용 |
|---|---|---|---|
| **MIT** | 허용 | 없음 | ✅ 허용 |
| **Apache 2.0** | 허용 | 없음 (특허 조항 주의) | ✅ 허용 |
| **BSD 2/3-Clause** | 허용 | 없음 | ✅ 허용 |
| **LGPL v2/v3** | 허용 | 라이브러리 수정분만 공개 | ⚠️ 링크 방식 확인 필요 |
| **GPL v2/v3** | 허용 | **전체 소스 공개 의무** | ❌ 사내 서비스에 원칙적 금지 |
| **AGPL v3** | 허용 | 네트워크 제공 시도 공개 의무 | ❌ 금지 (네트워크 서비스 포함) |
| **CC BY-SA** | 허용 | 동일 조건으로 공개 의무 | ❌ 소프트웨어 코드에 부적합 |
| **독점(Proprietary)** | 계약에 따름 | — | ⚠️ 개별 계약 확인 필요 |

> **GPL 전염성 주의**: GPL 라이브러리를 링크(의존성으로 추가)하면 해당 프로젝트 전체가 GPL의 영향을 받아 소스 공개 의무가 발생할 수 있다. AI가 GPL 라이브러리를 제안한 경우 반드시 대체 MIT/Apache 라이브러리를 검토한다.

#### AI 모델 관련 특수 라이선스 조건

오픈소스 AI 모델을 사내에 구축·활용하는 경우 아래 조건을 추가 확인한다.

| 모델 계열 | 특수 조건 | 확인 사항 |
|---|---|---|
| **Llama 3.1 이상** (Meta) | 월간 활성 사용자(MAU) 7억 명 초과 시 별도 라이선스 필요 | 서비스 규모 확인 |
| **Llama 3 이하** | 상업적 이용 별도 라이선스 요구 가능 | 버전별 라이선스 파일 확인 |
| **Stable Diffusion** | CreativeML Open RAIL-M — 특정 용도 금지 조항 | 사용 목적 적합성 확인 |
| **Mistral** | Apache 2.0 — 일반적으로 허용 | 최신 버전 라이선스 재확인 |

#### 라이선스 확인 절차

```
① AI가 신규 라이브러리 도입을 제안한 경우
   └─ 공식 저장소(GitHub, npm, Maven Central) 접속
   └─ LICENSE 파일 또는 package.json의 license 필드 확인

② 기존 의존성과 충돌 여부 확인
   └─ Java: `./gradlew dependencies` 출력에서 새 라이브러리 라이선스 확인
   └─ JavaScript: `license-checker --production` 실행

③ 불명확하거나 제한적 라이선스인 경우
   └─ AX추진실 또는 법무 담당 부서에 문의
   └─ 대체 라이브러리 검토
```

---

### 6.4 AI 생성 코드의 저작권 귀속

#### 업무상 생성 코드의 귀속 (요령 제20조 ①)

임직원이 **업무 목적**으로 AI 코딩도구를 이용하여 생성한 코드의 저작권 및 이용 권리는 **회사에 귀속**한다. 이는 임직원이 직접 작성한 코드와 동일한 원칙이다.

- 업무 시간 중, 또는 회사 업무와 관련하여 AI 도구로 생성한 코드 → 회사 귀속
- 회사 시스템·계정을 이용하여 생성한 코드 → 회사 귀속
- 개인 계정으로 순수 개인 목적으로 생성한 코드 → 개인 귀속 (단, 업무 관련성이 없는 경우에 한함)

#### 제3자 권리 침해 금지 (요령 제20조 ②)

AI 코딩도구를 이용하여 제3자의 아래 권리를 침해하는 방식으로 코드를 생성하거나 활용하여서는 아니 된다.

- **저작권**: 타인의 소스코드를 AI를 통해 표절·무단 복제하는 행위
- **영업비밀**: 경쟁사·협력사의 비공개 코드·알고리즘을 AI에 입력하여 유사 코드를 생성하는 행위
- **특허권**: 특허가 등록된 알고리즘을 AI를 통해 구현하는 행위

#### 대외 배포 시 부서장 사전 승인 (요령 제21조)

AI가 실질적으로 관여하여 생성한 코드·산출물을 아래 경우에 해당하도록 대외에 배포하려면 **부서장의 사전 승인**을 받아야 한다.

- 오픈소스로 외부 공개하는 경우
- 회원사·거래처에 소스코드 형태로 제공하는 경우
- 기술 블로그·컨퍼런스 등을 통해 공개하는 코드에 실질적으로 AI 생성 코드가 포함된 경우

---

### 6.5 AI 생성물 표시 의무

요령 제19조는 생성형 AI가 실질적으로 관여한 산출물을 대외 공개 시 그 사실을 표시할 것을 규정한다.

#### 코딩 업무에서의 표시 의무 적용 범위

| 산출물 유형 | 표시 의무 여부 |
|---|---|
| 사내 업무용 코드, 내부 배포 소프트웨어 | **면제** (내부 업무 문서 — 요령 제19조 ② 적용) |
| 회원사에 제공하는 API 문서, 기술 명세서 | **표시 필요** |
| 외부 공개 오픈소스 코드 | **표시 필요** (README 또는 주석 내 명시) |
| 기술 블로그·컨퍼런스 발표 자료 | **표시 필요** |
| 대외 서비스에 통합되는 AI 생성 응답 | **이용자에게 사전 고지** (요령 제19조 ③) |

#### 표시 방법

```markdown
<!-- 코드 파일 또는 README에 삽입 -->
> 이 코드(문서)는 생성형 AI의 지원을 받아 작성되었으며 담당자가 검토하였습니다.
```

---

### 6.6 AI 생성 코드 운영 적용 전 최종 체크리스트

아래 체크리스트를 코드 리뷰 또는 Pull Request 단계에서 확인한다.

```
[ AI 생성 코드 운영 적용 전 확인 ]
작성자: ___________  검토일: __________  PR/커밋: __________

기능 검증
  □ 요구사항을 올바르게 구현하는가?
  □ 단위 테스트가 통과하는가?
  □ 경계값·예외 케이스가 처리되어 있는가?

보안 검증
  □ 정적 분석 도구 실행 결과 이상 없음
  □ SQL 인젝션·XSS 등 OWASP Top 10 주요 취약점 해당 없음
  □ 하드코딩된 크리덴셜 없음 (git-secrets 또는 직접 확인)
  □ 신규 도입 라이브러리의 CVE 없음 (npm audit / Dependency-Check)

라이선스 확인
  □ 신규 도입 라이브러리 라이선스 확인 완료
  □ GPL·AGPL 등 전염성 라이선스 미포함 (또는 법무 검토 완료)
  □ AI 모델 특수 조건 해당 없음

저작권·귀속
  □ 제3자의 비공개 코드·알고리즘을 복제한 것이 아님
  □ 대외 배포 예정인 경우 부서장 사전 승인 완료

AI 생성물 표시 (대외 배포 시에만 해당)
  □ AI 관여 사실 표시 문구 삽입 완료 (또는 내부 문서로 면제 확인)
```

---

*— 다음: 제7장 개발 단계별 활용 가이드 —*

---

## 제7장 개발 단계별 활용 가이드

이 장은 분석·설계부터 문서화까지 개발 전 주기에 걸쳐 AI 코딩도구를 효과적으로 활용하는 실무 지침을 제공한다. 각 단계별로 권장 도구, 구체적인 활용 예시, 입력 시 주의사항을 함께 정리한다.

---

### 7.1 분석·설계 단계

**권장 도구**: Claude.ai (웹 채팅)  
**데이터 Level 주의**: 회원사 정보·미공개 사업 계획이 포함된 경우 Level 2 — 개인정보 마스킹 후 입력

분석·설계 단계는 코드보다 텍스트·다이어그램 중심 작업이 많아 웹 채팅 방식이 적합하다. 특히 여러 기술 옵션의 장단점 비교, 아키텍처 방향 검토, API 스펙 초안 작성에 활용도가 높다.

#### 활용 예시

**기술 스택·아키텍처 검토**
```
[권장 프롬프트]
"Spring Boot 3.x 기반 uTradeHub 배치 모듈을 개편하려고 합니다.
현재는 단일 스레드로 처리하고 있고 월 1,000만 건 이상 처리가 필요합니다.
Spring Batch vs Spring WebFlux 중 어떤 방향이 적합한지
처리량, 운영 복잡도, 팀 학습 비용 관점에서 비교해주세요."
```

**ERD 및 테이블 설계 검토**
```
[권장 프롬프트]
"아래 요구사항에 맞는 ERD를 제안해주세요.
- 회원사별 API 호출 이력을 일별로 집계
- 서비스명, 호출 건수, 응답시간(평균/최대), 오류 건수 저장
- 3개월 이상 데이터는 별도 아카이브 테이블로 이관
- 조회 패턴: 회원사별 최근 30일 통계가 가장 빈번"
```

**REST API 스펙 초안 작성**
```
[권장 프롬프트]
"다음 기능의 REST API 스펙을 OpenAPI 3.0 형식으로 초안 작성해주세요.
기능: 전자문서 제출 상태 조회
- 입력: 문서 ID (UUID), 신청자 사업자번호
- 출력: 제출 상태(대기/처리중/완료/오류), 처리 일시, 오류 코드(해당 시)
- 인증: Bearer JWT
- 오류 케이스: 문서 미존재, 권한 없음, 서버 오류"
```

#### 주의사항

- 설계 문서에 실제 회원사명·사업자번호·계약 조건이 포함된 경우 → 마스킹 후 입력하거나 "(회원사 A)", "(특정 거래처)" 등으로 치환
- AI가 제안한 아키텍처는 참고 자료로만 사용하고, 최종 설계 의사결정은 담당자가 직접 검토·확정

---

### 7.2 개발(코딩) 단계

**권장 도구**: JetBrains AI Pro / Cursor (인라인 완성·Agent), Claude Code (멀티파일·프로젝트 전체)  
**데이터 Level 주의**: 3장의 입력 금지 항목 준수 — 특히 크리덴셜, 인증·암호화 로직 제외

개발 단계는 AI 코딩도구의 생산성 향상 효과가 가장 직접적으로 나타나는 구간이다. 작업 규모에 따라 IDE 플러그인과 Claude Code를 구분하여 활용한다.

#### 작업 규모별 도구 선택 기준

| 작업 규모 | 권장 도구 | 이유 |
|---|---|---|
| 단일 메서드·함수 수정 | JetBrains AI / Cursor (인라인) | 빠른 인라인 제안, 컨텍스트 전환 최소화 |
| 단일 클래스·파일 작업 | JetBrains AI / Cursor (Agent) | 파일 내 컨텍스트 인식 우수 |
| 멀티파일 리팩토링 | Claude Code | 프로젝트 전체 구조 파악 후 일관된 변경 |
| 테스트 자동 생성 | Claude Code | 대상 코드 + 테스트 파일 동시 관리 |
| 신규 모듈·기능 구현 | Claude Code | 계획→구현→검증 루프 자율 수행 |

#### 활용 예시

**보일러플레이트 및 반복 코드 생성** (IDE 플러그인)
```
[JetBrains AI / Cursor 인라인 활용]
- Mapper 인터페이스 작성 후 탭 완성으로 MyBatis XML 쿼리 초안 생성
- DTO 클래스 선택 후 "Generate builder pattern" 주석 입력
- 테스트 클래스에서 @Test 메서드명 자동완성으로 기본 구조 생성
```

**리팩토링 — 중복 코드 정리** (Claude Code)
```
[권장 프롬프트]
"src/main/java/ktnet/common/ 하위의 유틸리티 클래스들을 분석해서
중복된 날짜 처리 메서드를 찾아 DateUtils 클래스로 통합해줘.
변경 범위: 해당 메서드를 사용하는 모든 호출부 수정 포함.
수정 전에 변경 계획을 먼저 보여주고 확인받아."
```

**단위 테스트 자동 생성** (Claude Code)
```
[권장 프롬프트]
"src/main/java/ktnet/invoice/InvoiceService.java의
calculateTax() 메서드에 대한 JUnit 5 단위 테스트를 작성해줘.
- 정상 케이스 3개 이상
- 경계값 케이스 (0원, 최대값)
- 예외 케이스 (null 입력, 음수 입력)
실제 DB 연결 없이 Mockito로 의존성 처리해줘."
```

**오류 원인 분석 및 수정** (Claude.ai 웹 채팅 또는 IDE 플러그인)
```
[권장 프롬프트]
"아래 스택 트레이스를 분석해서 원인과 수정 방향을 알려줘.
[스택 트레이스 붙여넣기 — 개인정보·크리덴셜 미포함 확인 후]
현재 환경: Spring Boot 3.2, JDK 17, Oracle 19c"
```

#### 개발 단계 주의사항

- IDE가 자동으로 참조하는 **열린 파일 목록**을 확인하고, `.env`·`application-prod.*` 파일이 열려 있지 않도록 한다.
- Claude Code에 대규모 작업을 지시하기 전 **반드시 CLAUDE.md**가 최신 상태인지 확인한다.
- AI가 생성한 코드는 **그대로 커밋하지 않고** 6장의 검증 절차를 이행한다.
- 복잡한 작업은 "수정 계획을 먼저 보여달라"고 요청하여 에이전트가 예상 범위 밖의 작업을 하지 않도록 사전에 확인한다.

---

### 7.3 코드 리뷰·테스트 단계

**권장 도구**: Claude.ai (웹 채팅), Claude Code  
**데이터 Level 주의**: 리뷰 대상 코드에 Level 3 항목(인증 로직, 크리덴셜)이 포함된 경우 — 3.4절 전처리 후 입력

#### 활용 예시

**코드 리뷰 지원** (Claude.ai 웹 채팅)
```
[권장 프롬프트]
"아래 코드를 리뷰해줘. 다음 관점에서 검토해줘.
1. 잠재적인 버그나 로직 오류
2. OWASP Top 10 기준 보안 취약점
3. 성능 개선 가능 포인트
4. Java 17 모범 사례(modern API 활용 여부)
[코드 붙여넣기 — Level 3 항목 제거 후]"
```

**테스트 커버리지 분석 및 보완** (Claude Code)
```
[권장 프롬프트]
"JaCoCo 리포트를 참고해서 커버리지가 낮은 클래스 상위 5개를 찾고,
누락된 테스트 케이스를 추가해줘.
리포트 경로: build/reports/jacoco/test/html/index.html"
```

**버그 재현 테스트 작성** (Claude.ai 웹 채팅)
```
[권장 프롬프트]
"다음 버그 시나리오를 재현하는 JUnit 테스트를 작성해줘.
버그: [증상 설명 — 회원사 특정 정보 제외]
예상 동작: [올바른 동작 설명]
실제 동작: [현재 잘못된 동작 설명]"
```

**성능 테스트 시나리오 설계** (Claude.ai 웹 채팅)
```
[권장 프롬프트]
"월 1,000만 건 배치 처리 모듈의 성능 테스트 시나리오를 설계해줘.
- 도구: JMeter 또는 k6
- 측정 지표: 처리 건수/초, 응답 시간(p95, p99), 오류율
- 부하 패턴: 점진적 증가(ramp-up) → 유지 → 감소"
```

#### 리뷰·테스트 단계 주의사항

- AI가 지적한 보안 취약점은 반드시 원인을 직접 확인하고 수정한다. AI의 지적이 오탐(False Positive)일 수 있으므로 맹신하지 않는다.
- AI가 생성한 테스트 코드도 **실제로 실행하여 통과 여부**를 확인한다. 구문상 올바르지만 실제로 의미 있는 검증을 하지 않는 테스트("형식만 있는 테스트")가 생성될 수 있다.

---

### 7.4 문서화 단계

**권장 도구**: Claude.ai (웹 채팅)  
**데이터 Level 주의**: 대외 배포 문서에 AI 관여 사실 표시 의무 적용 (요령 제19조, → 6.5절)

#### 활용 예시

**Javadoc·코드 주석 작성**
```
[권장 프롬프트]
"아래 메서드에 Javadoc 주석을 작성해줘.
- @param, @return, @throws 포함
- 메서드의 사이드 이펙트와 주의사항 포함
- 한국어로 작성
[메서드 시그니처 및 구현 붙여넣기 — Level 3 항목 제거 후]"
```

**REST API 기술 문서 작성**
```
[권장 프롬프트]
"아래 OpenAPI 스펙을 바탕으로 회원사 개발자를 위한 API 연동 가이드를 작성해줘.
- 인증 방법 (예시 포함)
- 요청/응답 예시 (curl, Java, Python)
- 자주 발생하는 오류 코드와 해결 방법
[OpenAPI YAML 붙여넣기]"
```

**장애 대응 런북(Runbook) 초안 작성**
```
[권장 프롬프트]
"아래 배치 시스템의 장애 대응 런북 초안을 작성해줘.
시스템명: [시스템명]
주요 장애 유형: DB 연결 실패, 파일 I/O 오류, 외부 API 타임아웃
각 장애 유형에 대해: 증상 → 원인 확인 방법 → 조치 절차 → 에스컬레이션 기준"
```

#### 문서화 단계 주의사항

- AI가 작성한 문서는 **담당자가 반드시 검토·수정**한 후 사용한다. AI는 사실과 다른 내용을 자신있게 서술할 수 있다.
- 대외 배포 문서(API 가이드, 기술 명세서 등)에는 6.5절의 AI 생성물 표시 문구를 포함한다.
- AI가 작성한 코드 주석이 실제 코드 동작과 일치하는지 확인한다. 주석만 보고 코드 리뷰를 통과시키면 안 된다.

---

### 7.5 금지 활용 사례

아래 사례는 이 가이드 및 상위 규범을 위반하는 활용 방식이다.

| 사례 | 위반 내용 | 근거 |
|---|---|---|
| UTH 인증 모듈 전체 코드를 Claude.ai에 붙여넣고 리뷰 요청 | Level 3(인증 로직) 입력 금지 | 요령 제16조 ①6호, 3.3절 |
| `.env` 파일을 그대로 AI에 붙여넣고 설정 분석 요청 | 크리덴셜 입력 금지 | 요령 제16조 ①7호, 3.3절 |
| 실제 회원사 사업자번호·수출입 데이터가 포함된 쿼리 결과를 AI에 입력 | Level 3(회원사 원문 데이터) 입력 금지 | 요령 제16조 ①1호, 3.1절 |
| Claude Code에 운영 DB 접속 MCP 서버를 연결하고 자율 작업 | AI 에이전트 사내 시스템 쓰기 사전 승인 미이행 | 요령 제42조 ①5호, 4.2절 |
| AI가 생성한 코드를 검증 없이 당일 운영 배포 | 코드 검증 의무 미이행 | 요령 제18조 ①, 6.1절 |
| 미승인 MCP 서버를 npm에서 내려받아 IDE에 연결 | 미승인 도구 사용 | 요령 제15조 ①, 2.4절 |
| GPL 라이선스 라이브러리를 사내 상업 서비스에 도입 | 오픈소스 라이선스 위반 | 요령 제20조 ③, 6.3절 |
| AI가 생성한 API 문서를 회원사에 제공하면서 AI 관여 사실 미표시 | AI 생성물 표시 의무 미이행 | 요령 제19조 ①③, 6.5절 |
| 승인 목록에 없는 개인 계정 AI 서비스로 업무 코드 질문 | 미승인 도구 사용 | 요령 제15조 ①, 2.4절 |
| Claude Code에 `git push origin main` 자동 승인 설정 후 방치 | AI 에이전트 외부 전송 사전 승인 미이행 | 요령 제42조 ①4호, 4.2절 |

---

*— 다음: 제8장 준수 의무·교육·사고 대응 —*

---

## 제8장 준수 의무·교육·사고 대응

이 장은 이 가이드의 준수를 담보하기 위한 교육 이수 의무, 자체 점검, 사고 발생 시 보고 절차, 위반 시 조치 기준을 정한다.

---

### 8.1 교육 이수 의무

요령 제47조에 따라 AI 코딩도구를 업무에 활용하는 임직원은 아래 교육을 이수하여야 한다. 코딩도구를 실제로 사용하는 기술 인력은 일반 임직원 교육 외에 **개발·운영 담당자 심화 교육(연 1회, 최소 4시간)** 이수 의무가 별도로 적용된다.

| 대상 | 교육 시기 | 최소 시간 | 비고 |
|---|---|---|---|
| 신규 입사자 (기술 직군) | 입사 후 **30일 이내** | 1시간 이상 | 온보딩 필수 과정 |
| 전 임직원 | 연 **1회** 이상 | 1시간 이상 | 연간 AI 교육계획에 통합 |
| **AI 개발·운영 담당 임직원** | 연 **1회** 이상 | **4시간 이상** | 본 가이드 대상자 전원 해당 |
| 임원 | 반기 **1회** | — | AI 거버넌스 위원회 보고 포함 |
| 협력업체 파견 인력 | 업무 수행 **전** | 1시간 이상 | 준수 서약서 제출 병행 |

#### 심화 교육 이수 내용 (기술 인력 기준)

기술 인력 대상 4시간 심화 교육은 아래 항목을 포함하여야 한다.

```
1부 (1시간) — AI 거버넌스 기초
  · 「인공지능 활용 및 관리 요령」 핵심 조항 이해
  · 도구 등급(Tier)·데이터 Level 분류 체계

2부 (1시간) — 코드·데이터 입력 기준 실습
  · Level 3 입력 금지 항목 식별 실습
  · 크리덴셜·핵심 로직 전처리 방법 실습

3부 (1시간) — AI 에이전트·MCP 보안
  · Claude Code 권한 통제 실습 (CLAUDE.md, Hooks 작성)
  · MCP 서버 보안 검토 체크리스트 적용

4부 (1시간) — 사고 예방·대응
  · 실제 위반 사례 분석
  · 사고 보고 절차 시뮬레이션
```

#### 교육 기록 관리

- 교육 이수 여부 및 이수 일시는 **3년간** 보관한다. (요령 제47조 ①)
- 협력업체 파견 인력의 준수 서약서 원본은 계약 기간 종료 후 **1년간** 보관한다.
- 미이수자는 AX추진실이 부서장에게 통보하며, 미이수 상태에서 AI 코딩도구를 계속 사용하는 경우 8.4절의 조치 대상이 된다.

---

### 8.2 자체 점검 (반기 1회)

각 부서 개발 리더는 반기 1회 아래 항목에 대한 자체 점검을 실시하고 결과를 AX추진실에 제출한다. (요령 제48조 ①)

#### 코딩도구 자체 점검 항목

```
[ AI 코딩도구 반기 자체 점검표 ]
부서명: ____________  점검 기간: ________ ~ ________  점검자: ____________

▣ 승인 도구 준수 여부
  □ 팀원 전원이 승인 목록 내 도구만 사용하고 있는가?
  □ 개인 계정(무료 플랜)으로 업무 목적 AI 사용 사례는 없는가?
  □ 미승인 IDE 플러그인 설치 사례는 없는가?

▣ 코드·데이터 입력 기준 준수
  □ 최근 반기 내 Level 3 코드(인증·암호화·핵심 로직) 입력 사고가 없었는가?
  □ 크리덴셜 실수 입력 사고가 없었는가?
  □ 전처리(마스킹·추상화) 방법이 팀 내 공유·실천되고 있는가?

▣ AI 에이전트(Claude Code) 통제
  □ 프로젝트별 CLAUDE.md가 최신 상태로 관리되고 있는가?
  □ Hooks 스크립트가 정상 동작하고 있는가?
  □ `git push` 등 외부 전송 작업에 자동 승인 설정이 없는가?

▣ MCP 서버·플러그인 관리
  □ 연결된 MCP 서버가 승인 목록 내 서버로 한정되어 있는가?
  □ AI-BOM이 현행화되어 있는가?
  □ EOL 또는 패치 미적용 플러그인·라이브러리가 없는가?

▣ 교육 이수 현황
  □ 팀원 전원이 당해 연도 심화 교육을 이수하였는가?
  □ 신규 입사자가 입사 30일 이내 교육을 이수하였는가?

점검 결과 요약: ___________________________________________________
개선 필요 사항: ___________________________________________________
AX추진실 제출일: __________
```

---

### 8.3 사고 보고 절차

AI 코딩도구와 관련하여 아래 보고 트리거에 해당하는 사건이 발생한 경우 즉시 보고 절차를 이행한다. (요령 제50조)

#### 코딩도구 주요 보고 트리거

| 사고 유형 | 구체적 상황 |
|---|---|
| **크리덴셜 입력** | API 키, 비밀번호, DB 접속 문자열 등을 외부 AI 도구에 실수로 입력 |
| **Level 3 코드 입력** | 인증·암호화·핵심 비즈니스 로직을 외부 AI 도구에 입력 |
| **회원사 원문 데이터 입력** | 수출입 신고 원문, 통관 내역, 개인정보가 포함된 데이터 입력 |
| **AI 에이전트 무단 작업** | Claude Code 등 에이전트가 승인 없이 운영 시스템을 변경하거나 데이터를 외부 전송 |
| **미승인 도구 사용으로 인한 유출** | 미승인 도구 또는 MCP 서버를 통한 업무 정보 유출 의심 |
| **프롬프트 인젝션 의심** | MCP 서버 또는 외부 데이터를 통해 에이전트 동작이 조작된 것으로 의심되는 상황 |

#### 시한별 보고 절차

```
사고 인지 즉시
  └─ [선행 조치]
     · AI 도구 화면에서 해당 대화(세션) 삭제 (가능한 경우)
     · 연결된 에이전트·MCP 서버 즉시 중단
     · 입력 내용, 도구명, 발생 시각 메모

인지 후 1시간 이내                                (요령 제50조 ①1호)
  └─ 직속 부서장 + AX추진실 사고대응 담당자에게
     전화 또는 사내 메신저로 1차 구두 보고
     (도구명, 입력 내용 요약, 발생 경위)

24시간 이내                                        (요령 제50조 ①2호)
  └─ 사고 보고서 작성·제출
     (「정보보호관리세칙」 소정 양식, AX추진실 제출)

72시간 이내                                        (요령 제50조 ①3호)
  └─ 개인정보가 포함된 사고인 경우
     개인정보보호위원회·KISA 등 감독기관 신고

7일 이내                                           (요령 제50조 ①4호)
  └─ 재발방지대책 수립 및 감사팀 제출

30일 이내                                          (요령 제50조 ①5호)
  └─ 사후점검 회의 개최
     허용 매트릭스·CLAUDE.md·Hooks 설정 개정 필요성 검토
```

#### AI 헬프데스크 연락처 (4채널)

문의 및 1차 보고는 아래 채널을 통해 한다. (요령 제8조 ④)

| 채널 | 연락처 | 응답 시간 |
|---|---|---|
| **사내 포털 Q&A 게시판** | 사내 포털 > AI 활용 > Q&A | 업무시간 내 24시간 이내 |
| **전용 이메일** | ai-help@ktnet.co.kr | 1영업일 이내 |
| **내선 전화** | 사내 공지 참조 | 업무시간 즉시 |
| **사내 메신저 AI 챗봇** | 사내 메신저 > AI 헬프데스크 | 24시간 (자동 응답) |

> **긴급 사고(크리덴셜·개인정보 유출 의심)**: 이메일·포털 대신 **전화 또는 메신저**로 즉시 연락한다. 이메일은 24시간 이내 응답을 보장하므로 긴급 상황에는 적합하지 않다.

---

### 8.4 위반 시 조치 기준

#### 조치 원칙

이 가이드 위반이 확인된 경우 「인사관리규정」 제50조부터 제56조의 징계 절차 및 양정에 따른다. 회사에 손해가 발생한 경우 민·형사상 책임이 별도로 부과될 수 있다. (요령 제49조 ⑥)

다음 원칙이 조치 과정에서 적용된다.

- **자진 보고 감경**: 위반 사실을 즉시 자진 보고하고 후속 조치에 성실히 협력한 경우 징계 양정에 감경 사유로 반영될 수 있다.
- **모니터링 기록 단독 불가**: 수집된 로그·모니터링 기록만을 유일한 근거로 하는 인사상 불이익 조치는 허용되지 않으며, 별도의 객관적 증거와 함께 인사위원회 심의를 거쳐야 한다. (요령 제45조 ③)
- **과실과 고의 구분**: 합리적인 주의를 기울였음에도 발생한 사고와 규정을 명백히 무시한 위반은 조치 수준이 다르게 적용된다.

#### 위반 유형별 조치 수준 (참고)

| 위반 유형 | 수준 | 주요 조치 |
|---|---|---|
| 경미한 절차 미준수 (교육 이수 지연, 자체 점검 보고 지연 등) | 경고 | 시정 요구, 기록 |
| 미승인 도구 사용 (정보 유출 없음) | 주의·경고 | 시정 요구, 추가 교육 이수 |
| Level 2 데이터 마스킹 미이행 입력 (단순 실수) | 경고 | 재발방지 서약, 추가 교육 |
| Level 3 데이터 입력 또는 크리덴셜 유출 (과실) | 감봉 이상 | 인사위원회 심의, 피해 복구 |
| 미승인 도구 사용으로 회원사 정보 유출 | 정직·해임 | 인사위원회 심의, 민·형사 책임 검토 |
| 고의적 정보 탈취·악용 | 해임 | 형사 고발 검토 |

> 위 수준은 참고 기준이며, 실제 조치는 사안의 경중, 피해 규모, 재발 이력, 협력 태도 등을 종합하여 인사위원회가 결정한다.

---

*— 다음: 부록 —*

---

## 부록

### [부록 A] 코딩도구 입력 판단 즉시 참조 카드

> 코드를 AI 도구에 붙여넣기 전 30초 점검. 하나라도 해당하면 입력 전 전처리 또는 중단.

```
┌────────────────────────────────────────────────────────────┐
│           ⚠️  AI 도구 입력 전 30초 체크                   │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  즉시 중단 (Level 3 — 모든 외부 AI 도구 입력 금지)        │
│                                                            │
│  □ API 키·비밀번호·DB 접속 문자열·인증서가 있는가?        │
│  □ 로그인·세션·토큰·암호화 처리 코드가 있는가?           │
│  □ UTH 등 핵심 시스템 비즈니스 로직이 있는가?            │
│  □ 실제 고객·직원 개인정보가 포함되어 있는가?            │
│  □ 운영 환경 설정 파일(.env, application-prod.*)인가?    │
│                                                            │
│  → 해당 있음: 전처리(치환·추상화·마스킹) 후 재시도      │
│               또는 ai-help@ktnet.co.kr 문의              │
│                                                            │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  조건부 허용 (Level 2 — 마스킹 후 Tier A 도구 사용 가능)  │
│                                                            │
│  □ 공개되지 않은 내부 업무 로직이 포함되어 있는가?       │
│  □ 내부 서버 IP·미공개 API 명세가 있는가?                │
│                                                            │
│  → 개인정보 마스킹, 민감 식별자 제거 후 입력 가능        │
│                                                            │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  자유 입력 (Level 1 — 모든 승인 도구 허용)                │
│                                                            │
│  □ 공개 라이브러리 사용 코드 (Spring, React 등)           │
│  □ 더미 데이터를 사용한 순수 로직 코드                   │
│  □ 알고리즘·자료구조 등 도메인 무관 코드                 │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

### [부록 B] 관련 규정 조항 색인

이 가이드의 각 기준이 근거하는 「인공지능 활용 및 관리 요령」(v1.0-r6) 조항 색인이다.

| 가이드 내용 | 요령 조항 | AI 도구 활용 정책 조항 |
|---|---|---|
| 도구 등급(Tier) 분류 | 제11조 | §3 도구 Tier 분류 |
| 데이터 민감도(Level) 분류 | 제12조 | §4 데이터 Level |
| 허용 매트릭스 | 제13조 | §5 허용 매트릭스 |
| 승인 도구 목록·사전 승인 | 제15조 | §8 도입 절차 |
| 소스코드 입력 금지 (인증·암호화·핵심 로직) | 제16조 ①6호 | §6 입력 금지 정보 |
| 크리덴셜 입력 금지 | 제16조 ①7호 | §6 입력 금지 정보 |
| 코드 검증 의무 | 제18조 | §7 결과물 검증 |
| AI 생성물 표시 의무 | 제19조 | §7 표시 |
| 저작권 귀속·라이선스 | 제20조 | §7 저작권 |
| 대외 배포 승인 | 제21조 | §7 배포 |
| AI 에이전트 권한 통제 | 제42조 | §9 에이전트 |
| 공급망 보안·MCP 서버·AI-BOM | 제43조 | §10 공급망 |
| 근로자 모니터링 권리 보호 | 제45조 | — |
| 교육 이수 의무 | 제47조 | §11 교육 |
| 자체 점검 | 제48조 | §11 점검 |
| 내부 감사·위반 조치 | 제49조 | §11 감사 |
| 사고 보고·대응 | 제50조 | §12 사고 대응 |
| AI 헬프데스크 연락처 | 제8조 ④ | — |
| 실험 샌드박스 (미적용) | 제9조 ⑤, 제46조 | — |

---

### [부록 C] 문서 이력

| 버전 | 일자 | 변경 내용 | 작성자 |
|---|---|---|---|
| v0.1 | 2026.06 | 초안 작성 (제1장~제8장, 부록) | AX추진실 |
| | | | |

---

*본 문서에 관한 문의: AX추진실 / ai-help@ktnet.co.kr*
